<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
    <link rel="stylesheet" type="text/css" href="/stylesheets/style.css">
    <style>
        body{
            background: linear-gradient(45deg, #49a09d, #5f2c82);
        }
        .navbar{
            background: linear-gradient(45deg, #49a09d, #5f2c82);
        }
    </style>
</head>
<body>
    <nav class="navbar" role="navigation" aria-label="main navigation">

    
        <div id="navbarBasicExample" class="navbar-menu">
            <div class="navbar-start id-link" >
                <a class="navbar-item" href="/" style="font-weight: bold">
                    Home
                </a>
                <% if(loggedIn && user?.isAdmin) {
                    %>
                    <a class="navbar-item" href="/orchids">
                        Orchids
                    </a>
                    <a class="navbar-item" href="/categories">
                        Categories
                    </a>
                    <a class="navbar-item" href="/accounts">
                        Accounts
                    </a>
                    <%
                } %>
            </div>
            <div class="navbar-end">
                <div class="navbar-item">
                    <div class="buttons">
                        <% if (loggedIn) { %>
                            <strong>Hi, <%= user.name %> </strong>
                            <a class="button is-link is-small" href="/profile" style="margin-left: 1rem;">
                                <strong><i class="fa fa-user"></i> Profile</strong>
                            </a>
                            <a class="button is-link is-small" href="/logout" style="margin-left: 1rem;">
                                <strong><i class="fa fa-sign-out-alt" aria-hidden="true"></i> Logout</strong>
                            </a>
                        <% } else { %>
                            <a class="button is-link is-small" href="/login">
                                <strong>Login</strong>
                            </a>
                            <a class="button is-link is-small" href="/register">
                                Register
                            </a>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </nav>
</body>
</html>